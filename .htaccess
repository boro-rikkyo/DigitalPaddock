<IfModule mod_rewrite.c>
  RewriteEngine On
  # 拡張子なしのファイルへのアクセスを .html に転送
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME}.html -f
  RewriteRule ^(.*)$ $1.html [L]
</IfModule>
